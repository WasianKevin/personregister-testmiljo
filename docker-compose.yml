services:
  user_service:
    build: .
    container_name: user-gdpr-manager
    volumes:
      - userdb_volume:/storage/db
    environment:
      - USER_DB_PATH=/storage/db/sample_users.db

volumes:
  userdb_volume: